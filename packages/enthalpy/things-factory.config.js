import bootstrap from './dist-client/bootstrap.js'
import route from './dist-client/route.js'

export default {
  route,
  routes: [
    {
      tagname: 'evaluator-home',
      page: 'evaluator-home'
    }
  ],
  bootstrap
}
